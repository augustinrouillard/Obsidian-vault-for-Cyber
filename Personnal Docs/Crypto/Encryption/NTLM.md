---
share: true
---
**NTLM** is a challenge-response authentication protocol that verifies a user's identity without transmitting their password in plain text. The password is processed using a hash function to generate a **hash**, which is then used in the authentication process. This authentication process is commonly used in Windows systems for network services, access to shared files, etc.